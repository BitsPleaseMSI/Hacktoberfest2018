// TASK
// Complete the code to check if the character is vowel or Consonant

#include <stdio.h>
int main()
{
    char ch;
    bool isVowel = false;

    printf("Enter an alphabet: ");
    scanf("%c",&ch);

    if()
    {
    	isVowel = true;

    }
    if ()
        printf("%c is a Vowel", ch);
    else
        printf("%c is a Consonant", ch);
    return 0;
}
