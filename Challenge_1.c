// TASK
// Copy the string from dynamic array foo to dynamic array bar.


#include <stdlib.h>
#include <string.h>

int main () {
        char * foo = "hello";
        //complete the code
        char * bar = ;
        strcpy(bar, foo);
        printf("%s",bar);
        return 0;
}
