// TASK
// Write code to find if the number is present in the array or not
// Code Below
#include<stdio.h>
#include<conio.h>
void main()
{int A[5]={1,4,6,57,34},i,flag=0,notno=6;
 for (i=0;i<5;i++)
 {if (no==A[0])
  {flag=1;
   break;
  }
 }if (flag==1)
   printf("no. found");
  else if (flag==0)
   printf("No. not found");\
 getch();
}
