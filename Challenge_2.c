// TASK
//
// 1. Create an array in function f() and g() of size 3
// 2. assign the array to a and b respectively

#include <stdio.h>
int *a, *b;

void f(int x) {

}

void g(int x) {

}

int main() {
  f(1);
  printf("a = {%d,%d,%d}\n", a[0], a[1], a[2]);
  g(2);
  printf("a = {%d,%d,%d}\n", a[0], a[1], a[2]);
  return 0;
}
